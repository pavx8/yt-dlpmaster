# yt-dlpMaster

Десктопный GUI для `yt-dlp`, написанный на Python и PySide6.

Русский: `README.ru.md` | [English](README.md)

## Key Features
| Статус | Возможность |
| --- | --- |
| ✅ | Десктопный GUI на `Python + PySide6` |
| ✅ | Ввод URL, выбор папки сохранения и базовые пресеты загрузки |
| ✅ | Анализ URL перед скачиванием: название, длительность, превью, обновление пресетов |
| ✅ | Скачивание в фоне с прогрессом, статусом и логом |
| ✅ | Поддержка прокси (`socks4`, `socks5`, `socks5h`, `http`, `https`) |
| ✅ | Поддержка cookies из браузера и `cookies.txt` |
| ✅ | Тест cookies из интерфейса |
| ✅ | Опциональное перекодирование через `ffmpeg` |
| ✅ | Извлечение аудио и конвертация в `mp3` / `opus` / `aac` |
| ✅ | Интеграция `SponsorBlock` |
| ✅ | Переключение между встроенными и системными компонентами |
| ✅ | Настройки языка, темы и поведения окна |
| ✅ | Системный трей и сворачивание при закрытии |
| ✅ | Встроенный обновлятор `yt-dlp`, `yt-dlp-ejs`, `ffmpeg`, `certifi` |
| ✅ | Базовая локализация на двух языках: `ru_RU` / `en_US` |
| ⬜ | Продвинутая локализация через WebLate |
| ⬜ | Очередь загрузок и несколько URL за раз |
| ⬜ | Загрузка плейлистов |
| ⬜ | Пауза, возобновление, отмена и повтор загрузки |
| ⬜ | История загрузок и быстрые действия с файлами |
| ⬜ | Экспорт логов и расширенная диагностика ошибок |
| ⬜ | Автообновление самого приложения |
| ⬜ | Готовые релизные сборки (`AppImage`, `exe`, portable bundle`) |

## Roadmap
| Направление | Пункт | Статус |
| --- | --- | --- |
| Foundation | Базовый GUI для `yt-dlp` | ✅ |
| Foundation | Анализ ссылки и превью | ✅ |
| Foundation | Фоновые загрузки и логирование | ✅ |
| Foundation | Настройки, локализация, tray, updater компонентов | ✅ |
| Reliability | Unit-тесты для `settings`, `cookies`, `proxy`, `format fallback` | ⬜ |
| Reliability | Smoke/integration-тесты для анализа и скачивания | ⬜ |
| Reliability | CI для `lint`, тестов и сборки переводов | ⬜ |
| Reliability | Более детализированные ошибки для auth/network/DRM/ffmpeg | ⬜ |
| Downloads UX | Очередь задач | ⬜ |
| Downloads UX | Несколько URL и поддержка плейлистов | ⬜ |
| Downloads UX | Pause/Resume/Cancel/Retry | ⬜ |
| Downloads UX | История загрузок с кнопками "открыть файл" и "открыть папку" | ⬜ |
| Formats | Ручной выбор доступных форматов | ⬜ |
| Formats | Показ контейнера, кодеков и примерного размера до скачивания | ⬜ |
| Formats | Опции для субтитров, metadata, thumbnail и naming templates | ⬜ |
| Release | Упаковка релизов для Linux/Windows | ⬜ |
| Release | Обновление самого приложения | ⬜ |
| Release | Более подробная пользовательская документация | ⬜ |

## Требования
- Python `>=3.10,<3.15`
- Poetry
- `ffmpeg` в `PATH` или комплектные бинарники в проекте

## Быстрый старт
```bash
poetry install
poetry run yt-dlpmaster
```

Запуск с явным языком интерфейса:
```bash
poetry run yt-dlpmaster --lang ru_RU
```

## Локализация
- Исходники переводов находятся в `app/i18n/*.ts`.
- Бинарные файлы переводов - `app/i18n/*.qm`.
- При старте приложения `.qm` собираются автоматически, если отсутствуют или старее `.ts`.

Ручные команды:
```bash
./scripts/build_translations.sh
./scripts/update_translations.sh
```

## Структура проекта
- `app/cli.py` - точка входа CLI (`yt-dlpmaster`)
- `app/ui/` - слой Qt-интерфейса
- `app/core/` - загрузка, ffmpeg, настройки, CA-хелперы
- `app/i18n/` - локализации (`.ts` / `.qm`)
- `scripts/` - утилиты для работы с переводами

## Примечания
- Настройки хранятся в портативном режиме в `settings.ini` в корне проекта.
- При включенной опции приложение предпочитает встроенные бинарники/библиотеки.
- Поиск `ffmpeg` сначала проверяет локальные комплектные пути, затем системный `PATH`.
